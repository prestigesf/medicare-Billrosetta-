The file `NOT_A_REAL_ASSIGNMENT.txt` is a test fixture only.
It exists so `tools/build_borrowing_base.py --assignment-file` can exercise the titled math path.
It is not an assignment of Medicare benefits. Eligible AR without a real assignment file is $0.
